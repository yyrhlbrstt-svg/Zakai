# LAUNCH — המדריך המלא להעלאה לאוויר ול-10 המשתמשים הראשונים

*הקוד מוכן. המסמך הזה הוא כל מה שנשאר בין ה-branch לבין משתמשים אמיתיים.
כל צעד כאן הוא פעולה של המייסד (דברים שדורשים חשבונות וסיסמאות שרק לו יש).*

---

## שלב 1 — לפרוס לפרודקשן (10 דקות)

1. היכנס ל-Vercel → פרויקט **zakai-3uxj** → Deployments.
2. מצא את הפריסה האחרונה של `claude/zakai-consumer-ai-agent-3ucncj` (ירוקה ✓).
3. תפריט ⋯ → **Promote to Production**.
4. פתח `https://zakai-3uxj.vercel.app/he` בחלון גלישה בסתר וודא:
   - דף הבית עולה עם 3 קטגוריות הכלים.
   - `/he/faq`, `/he/lost-money`, `/he/results` עולים.
   - אין באנר Vercel (אם יש — Settings → Deployment Protection → כבוי).

> הפריסה מריצה `prisma migrate deploy` אוטומטית — המיגרציה של מצב-משפחה
> (`beneficiaryLabel`) תיושם לבד. אם ה-build נכשל על DB — לבדוק את
> `DATABASE_URL` ב-Environment Variables.

## שלב 2 — לחבר את המוח (5 דקות)

1. Vercel → Settings → **Environment Variables**.
2. הוסף `GEMINI_API_KEY` = המפתח מ-AI Studio (זה שעל **Tier 1 · Prepay**,
   מושך מה-₪849 קרדיט). Production ✓.
3. אופציונלי: `GEMINI_ASSISTANT_MODEL` = `gemini-2.5-pro` (זו כבר ברירת
   המחדל בקוד; המשתנה קיים לעקיפה).
4. Deployments → ⋯ → **Redeploy** (חובה אחרי שינוי env).
5. בדיקה: `/he/assistant` → לשאול "מה מגיע לי אם פוטרתי אחרי שנתיים?"
   ולוודא תשובה עניינית עם מקור רשמי.

## שלב 3 — 10 המשתמשים הראשונים (השבוע)

הלינק לשליחה: **`https://zakai-3uxj.vercel.app/he`**

מי הכי מתאים (לפי הוורטיקלים החזקים שלנו):
| מי | למה | לאיזה מסך לכוון |
|---|---|---|
| בן משפחה שמשלם הרבה על סלולר | המסלול המלא (ייפוי כוח → חיסכון מתועד) | `/check` |
| מי שהחליף עבודות / עבד חלק משנה | החזר מס — הוורטיקל עם ה"וואו" הכי מהיר | `/taxrefund` |
| מי ששירת מילואים מאז 2023 | תוספת ה-20% שכמעט אף אחד לא מכיר | `/miluim` |
| הורה/סבא עם חשבונות ישנים | כסף אבוד — הר הכסף והר הביטוח | `/lost-money` |
| כל אחד אחרי טיסה עם עיכוב | פיצוי טיסה/כבודה | `/flights`, `/baggage` |

הודעת ההשקה (להעתקה לוואטסאפ):
> בניתי אפליקציה שבודקת בחינם אם מגיע לך כסף — החזרי מס, זכויות,
> פיצויי טיסות, כסף אבוד בפנסיה, והוזלת חשבונות. בלי הרשמה לרוב
> הבדיקות. תנסו ותגידו לי מה מצאתם: https://zakai-3uxj.vercel.app/he

מה לבקש מכל אחד מה-10:
1. איזה מסך פתחת ראשון? מה לא היה ברור?
2. מצאת משהו ששווה כסף? כמה?
3. היית משתף את זה עם מישהו? (אם כן — שישתפו מה-dashboard: השיתוף
   נושא את קוד ההפניה שלהם אוטומטית.)

## שלב 4 — ברגע שיש חיסכון מתועד ראשון

- עמוד `/results` יתחיל להציג מספרים אמיתיים אוטומטית (מהיומן, לא ידני).
- לבקש מהמשתמש משפט עדות + אישור לפרסם (שם פרטי בלבד) — העדות הראשונה
  שווה יותר מכל פיצ'ר.
- לפתוח את שיחת ה-PSP (ספק סליקה ישראלי) — התנאי לגביית עמלה אמיתית.
  דרישות מקדימות: ישות עסקית (עוסק/חברה) + חשבון בנק עסקי.

## מדדי הצלחה לשבועיים הראשונים

- 10 משתמשים נכנסו · 5 הריצו בדיקה כלשהי · 1 חיסכון מתועד ביומן.
- ברגע שהושג ה-1 — חוזרים למנוע ההנדסי: בונים את שכבת הסליקה ואת
  הוורטיקל שהמיר הכי טוב.
